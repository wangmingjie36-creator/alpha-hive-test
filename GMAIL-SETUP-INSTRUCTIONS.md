# 📧 Gmail 邮件告警配置指南

**目标**: 在 5 分钟内启用 Gmail 邮件通知

---

## 📋 步骤 1: 检查你的 Gmail 账户

### 要求
- ✅ 已有 Gmail 账户
- ✅ 启用了两步验证（2FA）
- ❌ 不能使用普通密码（安全限制）

---

## 🔑 步骤 2: 生成 Gmail 应用密码

这是关键步骤！**应用密码** 与账户密码不同，安全性更高。

### 2.1 访问 Gmail 安全设置

```
访问：https://myaccount.google.com/security
或
在 Gmail 中点击：右上角头像 → Google 账户 → 左侧"安全"
```

### 2.2 检查两步验证

```
在"安全"页面找到"两步验证"
- 如果已启用：✅ 跳到下一步
- 如果未启用：⚠️ 需要先启用（点击"启用"按钮）
```

### 2.3 生成应用密码

```
在"安全"页面找到"应用专用密码"
└─ 点击"应用专用密码"
   ├─ 选择应用：选择"邮件"
   ├─ 选择设备：选择"Windows PC" 或 "Mac"
   └─ 点击"生成"

Google 会生成一个 16 位密码，例如：
   abcd efgh ijkl mnop
```

### 2.4 复制密码

```
✨ 重要：复制生成的应用密码（不要复制中间的空格）
    实际密码：abcdefghijklmnop

⚠️  这个密码只会显示一次！
   如果丢失了，需要重新生成
```

---

## ⚙️ 步骤 3: 配置 Alpha Hive

现在你已有应用密码，下面配置 Alpha Hive 来使用它。

### 3.1 修改 config.py

打开文件：`/Users/igg/.claude/reports/config.py`

找到 `ALERT_CONFIG` 部分（在末尾），修改如下：

```python
ALERT_CONFIG = {
    # 启用邮件通知
    "email_enabled": True,  # ← 改为 True

    "email_config": {
        "smtp_server": "smtp.gmail.com",        # Gmail SMTP 服务器
        "smtp_port": 587,                       # Gmail SMTP 端口
        "sender_email": "your-email@gmail.com", # ← 改为你的 Gmail
        "sender_password": "abcdefghijklmnop",  # ← 改为应用密码（无空格）
        "recipient_emails": ["your-email@gmail.com"],  # ← 接收邮件的地址
        "use_tls": True
    },

    # 其他配置保持不变
    "slack_enabled": False,
    ...
}
```

### 3.2 验证配置

检查你的修改：
- ✅ `email_enabled`: `True`
- ✅ `smtp_server`: `"smtp.gmail.com"`
- ✅ `sender_email`: 你的 Gmail 地址
- ✅ `sender_password`: 应用密码（**无空格**）
- ✅ `recipient_emails`: 接收告警的邮箱

---

## 🧪 步骤 4: 测试邮件通知

### 4.1 快速测试

```bash
# 运行邮件通知测试
python3 /Users/igg/.claude/reports/email_notifier.py

# 输出应该显示：
# Email Notifier Test
# To enable email notifications:
# 1. Add email config to config.py...
```

### 4.2 完整告警测试

```bash
# 测试完整的告警分析 → 邮件发送流程
python3 /Users/igg/.claude/reports/alert_manager.py \
    --status-json /Users/igg/.claude/reports/status.json \
    --output-dir /Users/igg/.claude/logs \
    --dispatch  # ← 这会尝试发送邮件

# 预期结果：
# ✅ Alerts saved: ...
# ✅ Dispatch result:
#    Sent: 1
#    Failed: 0
```

### 4.3 检查邮件

```
⏰ 等待 1-5 秒
📧 检查你的邮箱（包括垃圾箱）

应该收到来自 sender_email 的邮件，主题类似：
   [HIGH] Step Failed: ...
或 [MEDIUM] Low Opportunity Scores
```

---

## 🚀 步骤 5: 完整演示

现在让我们运行完整的编排流程，看看邮件告警的实际效果：

### 5.1 手动触发完整流程

```bash
bash /Users/igg/.claude/scripts/alpha-hive-orchestrator.sh "NVDA TSLA VKTX"

# 这会：
# ✅ 执行 Step 1-5（数据采集、分析、报告）
# ✅ 执行 Step 6（自动告警分析）
# ✅ 如果有告警 → 自动发送邮件
```

### 5.2 监控日志

```bash
# 实时查看执行日志
tail -f /Users/igg/.claude/logs/orchestrator-$(date +%Y-%m-%d).log

# 查看告警详情
cat /Users/igg/.claude/logs/alerts-$(date +%Y-%m-%d).json | jq .
```

### 5.3 检查邮件

```
📧 邮件应该包含：
   ├─ 告警标题 (P0/P1/P2 级别)
   ├─ 详细信息表格
   │   ├─ Alert Level
   │   ├─ Time
   │   └─ 其他关键数据
   ├─ 标签 (tags)
   └─ Alpha Hive Alert System 署名
```

---

## 📊 邮件示例

### 立即发送（P0/P1 级别）

```
主题: [HIGH] Step Failed: step2_hive_analysis

内容:
┌─────────────────────────────────────┐
│ Alert Level: HIGH                   │
│ Time: 2026-02-24T10:30:00           │
│                                     │
│ Details:                            │
│ ├─ step: step2_hive_analysis       │
│ ├─ duration: 45 seconds             │
│ └─ status: FAILED                   │
│                                     │
│ Tags: step_failure, pipeline        │
└─────────────────────────────────────┘
```

### 汇总发送（P2 级别，每小时一次）

```
主题: [Alpha Hive] Daily Alert Digest (3 alerts)

内容:
┌────────────────────────────────────────┐
│ 🐝 Alpha Hive Daily Alert Digest      │
│                                        │
│ Time: 2026-02-24 10:35:00             │
│ Total Alerts: 3                        │
│ Critical: 0                            │
│ High: 0                                │
│ Medium: 3                              │
│                                        │
│ 1. [MEDIUM] Low Opportunity Scores    │
│    Time: 2026-02-24T10:25:00          │
│    • top_score: 5.1/10                │
│    • avg_score: 5.1/10                │
│                                        │
│ 2. [MEDIUM] Very Low Top Opportunity  │
│    ...                                 │
│                                        │
│ 3. [MEDIUM] GitHub Deployment Failed  │
│    ...                                 │
└────────────────────────────────────────┘
```

---

## ✅ 故障排除

### 问题 1: 邮件发送失败，提示"Authentication failed"

**原因**: 密码错误或配置错误

```python
# 检查清单：
1. ✅ 使用的是"应用密码"，不是 Gmail 账户密码
2. ✅ 应用密码中的空格已移除
3. ✅ sender_email 是正确的 Gmail 地址
4. ✅ 两步验证已启用
```

**解决方案**:
```bash
# 重新生成应用密码
访问：https://myaccount.google.com/apppasswords
# 删除旧密码，生成新密码
# 复制新密码到 config.py，重新测试
```

### 问题 2: 邮件没有收到

```
检查清单：
1. ✅ 收件箱中是否有邮件
2. ✅ 检查垃圾箱（可能被标记为垃圾）
3. ✅ 检查 recipient_emails 是否配置正确
4. ✅ 查看执行日志是否有错误
```

### 问题 3: config.py 语法错误

```python
# 常见错误：
❌ "sender_password": "abc def ghi"  # 有空格
✅ "sender_password": "abcdefghi"    # 无空格

❌ "sender_email": your-email@gmail.com  # 缺少引号
✅ "sender_email": "your-email@gmail.com"  # 有引号
```

---

## 🎯 现在你可以

✅ **接收 P0/P1 级别的实时告警邮件**
- 系统故障立即通知
- 性能异常立即通知
- 步骤失败立即通知

✅ **每小时收到一份汇总邮件**
- 包含所有 P2 级别告警
- 可自定义发送时间

✅ **完全可配置**
- 修改告警规则
- 调整性能阈值
- 启用/禁用特定告警类型

---

## 📝 快速参考

### 启用邮件通知

```bash
# 1. 生成 Gmail 应用密码 (上述步骤 2)
# 2. 修改 config.py 的 email_config (上述步骤 3)
# 3. 测试邮件
python3 alert_manager.py --dispatch --test-mode
# 4. 运行编排脚本
bash /Users/igg/.claude/scripts/alpha-hive-orchestrator.sh
```

### 关键配置

```python
# 关键参数检查清单
email_enabled: True                      # ✅ 已启用
smtp_server: "smtp.gmail.com"            # ✅ Gmail SMTP
smtp_port: 587                           # ✅ Gmail 端口
sender_email: "your-gmail@gmail.com"     # ✅ 你的邮箱
sender_password: "abcdefghijklmnop"      # ✅ 应用密码（无空格）
use_tls: True                            # ✅ 加密传输
```

---

## 🎉 你已准备好了！

✨ 邮件告警系统现已激活

**下一步**: 运行完整流程或继续 Phase 2 Week 2 (性能监控)

---

**有问题？** 查看"故障排除"部分或告诉我具体错误信息

